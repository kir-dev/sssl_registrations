<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">SSSL Képzés Admin</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <% if logged_in_as_accepted? %>
          <li class="nav-item">
            <%= link_to 'Regisztrációk', registrations_path, class: 'nav-link' %>
          </li>
        <% end %>
        <% if logged_in_as_admin? %>
          <li class="nav-item">
            <%= link_to 'Felhasználók', users_path, class: 'nav-link' %>
          </li>
        <% end %>
        <% if logged_in? %>
          <li class="nav-item">
            <%= link_to 'Kijelentkezés', logout_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>